{
  "name": "pdf-generation-server",
  "version": "1.0.0",
  "description": "Edge Function to generate PDF reports",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build script needed'",
    "prebuild": "apt-get update && apt-get install -y wkhtmltopdf" // Replace with appropriate command
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^28.4.17"
  }
}
