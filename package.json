{
  "name": "pdf-generation-server",
  "version": "1.0.0",
  "description": "Serverless Function to generate PDF reports",
  "main": "api/generate-pdf.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build script needed'"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^10.1.0"
  },
  "devDependencies": {
    "vercel": "^28.4.17"
  }
}
